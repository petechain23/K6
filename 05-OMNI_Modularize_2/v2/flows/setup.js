// v2/setup.js (Example - can be empty if not needed)
export function setup() {
    console.log("Executing global setup tasks (if any)...");
    // Example: Fetch global config, prepare shared resources
    return { globalConfig: { setting: 'value' } }; // Data returned is passed to VU functions
  }
  